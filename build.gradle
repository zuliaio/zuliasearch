apply plugin: 'java'

defaultTasks 'build'
subprojects {

    apply plugin: 'java'

    sourceCompatibility = 1.8
    targetCompatibility = 1.8

    version = '1.0'
    ext.isReleaseVersion = !version.endsWith("SNAPSHOT")

    group = 'io.zulia'

    repositories {
        mavenCentral()
    }

}
