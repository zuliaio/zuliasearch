<configuration>

    <!-- Keeps JUL levels in sync with logback so log statements disabled here are cheap at the JUL side too. -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>

    <!-- Colored pattern on an interactive terminal, plain pattern otherwise. See ConsoleLogPatternDefiner. -->
    <define name="ZULIA_LOG_PATTERN" class="io.zulia.cmd.common.ConsoleLogPatternDefiner"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>${ZULIA_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
